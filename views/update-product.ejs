<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Update Product</title>
  </head>
  <body>
    <h1>Update Product</h1>
    <form method="put" id="update_product">
      <label for="name">Name:</label>
      <input type="text" id="name" name="name" value="<%= product.name %>">
      <label for="price">Price:</label>
      <input type="number" id="price" name="price" min="0" value="<%= product.price %>">
      <label for="description">Description:</label>
      <textarea id="description" name="description"><%= product.description %></textarea>
      <label for="categories">Categories:</label>
      <select id="categories" name="categories">
        <option value="">Select a category</option>
        <option value="eletronics" <% if (product.categories === "phone") { %>selected<% } %>>Phone</option>
        <option value="beauty products" <% if (product.categories === "watches") { %>selected<% } %>>Watches</option>
        <option value="toys" <% if (product.categories === "toys") { %>selected<% } %>>Toys</option>
      </select>
      <input type="submit" value="Update">
    </form>
  </body>
</html>